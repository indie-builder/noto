# Noto 全项目设计交付

目标：依照 Emil Design Engineering 先制定规范，再逐界面逐功能实现并完成端到端验证。

## 里程碑

- [x] G1 盘点：README、全部 App 源码、现有设计稿、已有验证和核心测试
- [x] G2 设计规范：`DESIGN-SYSTEM.md`（视觉 token、组件、状态、动效、页面功能矩阵）
- [x] G3 主输入、列表、日期、搜索、反馈改造
- [x] G4 编辑、设置、AI 对话与响应式改造
- [x] G5 编译、核心测试、CLI 回归
- [x] G6 原生应用端到端操作、截图与修复
- [x] G7 交付可运行 Noto.app、验证报告与剩余限制

## 端到端验收清单

| 流程 | 验收 | 结果 |
| --- | --- | --- |
| 记录 | 输入 → 回车保存 → 列表可见 → 重开/CLI 可查 | 通过；24 行长输入也完成保存 |
| 待办 | 添加 → 完成 → 撤销 → 编辑截止日期 | 通过；按钮与 ⌘⇧ 回车均验证 |
| 搜索 | ⌘K → 搜索记录/对话 → 无结果 → 清空 | GUI 记录/无结果/最早历史通过；对话搜索由核心测试覆盖 |
| 日期 | 展开/收起 → 日期跳转 → 分页 | 通过；110 条、55 天，到达 7 月 16 日 |
| 编辑 | 打开 → 保存 → 空文本禁用 | 通过；原生日期选择和 ⌘ 回车保存 |
| 设置 | 打开 → 选择 → 关闭与持久化 | 通过；Codex 重开保留，OpenCode/Kimi 切换 |
| 对话 | 发起 → 执行过程 → 真实回复或错误 → 重试/停止 → 重开与草稿 | 通过；Kimi 两轮真实回复，4 条消息，1 条待办，无重复创建 |
| 视觉 | 主窗口、紧凑尺寸、弹窗、对话与明暗外观 | 通过本次采样；见 QA 报告中的覆盖与限制 |
| 回归 | 核心测试、构建、独立数据库 CLI | 4 项测试通过；Release 构建与 CLI 回归通过 |

原始源码备份：`redesign/NotoApp.before.swift.txt`。历史 QA 不作为本次验证结果。

交付文件：`../build/Noto.app`。



